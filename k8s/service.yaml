apiVersion: v1
kind: Service
metadata:
  name: django-service
  namespace: django-app
spec:
  type: NodePort
  selector:
    app: django
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP