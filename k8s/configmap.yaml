apiVersion: v1
kind: ConfigMap
metadata:
  name: django-config
  namespace: django-app
data:
  DEBUG: "False"
  ALLOWED_HOSTS: "*"
  POSTGRES_DB: "django_db" 
  POSTGRES_USER: "django_user"
  POSTGRES_HOST: "postgres-postgresql.django-app.svc.cluster.local" 
  POSTGRES_PORT: "5432"